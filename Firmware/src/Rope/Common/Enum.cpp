#include "Rope/Common/Enum.hpp"

namespace Rope
{
    // Enum::Enum(const std::string& typeName, const std::string& name, uint32_t value) :
    //     NamedEntity(name),
    //     typeName(typeName),
    //     value(value)
    // {

    // }

    // bool Enum::operator==(const Enum& other) const
    // {
    //     return other.typeName == typeName && other.value == value;
    // }

    // bool Enum::operator!=(const Enum& other) const
    // {
    //     return other.typeName != typeName || other.value != value;
    // }

    // uint32_t Enum::getValue() const
    // {
    //     return value;
    // }
}