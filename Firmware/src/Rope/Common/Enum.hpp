#ifndef ROPE_COMMON_ENUM_HPP
#define ROPE_COMMON_ENUM_HPP

#include <stdint.h>
#include "Rope/Common/NamedEntity.hpp"

namespace Rope
{
    class Enum// : public NamedEntity
    {
    // private:
    //     std::string typeName;
    //     uint32_t value;

    //     Enum() = default;

    // protected:
    //     Enum(const std::string& typeName, const std::string& name, uint32_t value);

    // public:
    //     explicit operator bool() = delete;

    //     virtual bool operator==(const Enum& other) const final;
    //     virtual bool operator!=(const Enum& other) const final;

    //     virtual uint32_t getValue() const final;
    };
}

#endif // ROPE_COMMON_ENUM_HPP